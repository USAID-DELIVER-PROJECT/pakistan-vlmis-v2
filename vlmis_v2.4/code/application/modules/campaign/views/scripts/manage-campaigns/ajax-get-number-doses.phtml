<?php
if ($this->data != false) {
    echo $this->data['0']['numberOfDoses'];
    // $number_of_doses = $this->data['0']['numberOfDoses'] * $this->vials_used;
    // $coverage_not_accessible = $this->coverage_not_accessible;
    // if ($coverage_not_accessible <= $number_of_doses ) {
    //      echo 'Covered should not be greater than vials used';
    // } else {
    //     
    // }
}
?>